import {createTheme} from "@mui/material/styles";

const theme = createTheme({
    typography: {
        fontFamily: [
            'IBM Plex Sans JP',
            'sans-serif',
        ].join(','),
    },
});
export default theme;